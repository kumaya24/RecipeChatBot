# RecipeChatBot
Capstone Project

## Set Up
### Backend
At the root folder, run command `fastapi dev app.py` to start backend server. Then run Elasticsearch in your Docker, and activate Ollama on your terminal by command `ollama serve`.
### Frontend
Navigate to the `frontend` folder, install all dependencies by running `npm install`, and then start the frontend server with `npm run dev`.
