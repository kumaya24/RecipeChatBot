# RecipeChatBot
Capstone Project

## Frontend
### Set Up
Navigate to the `frontend` folder, install all dependencies by running `npm install`, and then start the development server with `npm run dev`.
